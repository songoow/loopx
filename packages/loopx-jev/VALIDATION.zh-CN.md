# 验证结果与声明边界

[English](VALIDATION.md)

基于原上游 `04ba65ac1a37c94d964adeedf3ff1f125c5ffe12` 和个人试点分支继续开发。
这不代表上游采用，也不授权修改活动 Goal。

D1–D6 已有显式 `loopx-jev assess` 入口，读取调用者声明的版本化输入和本地证据。
这是可使用的有限辅助入口，不是自动接入所有上游 owner。D7/D8 保留真实 scoped
fallback selector 和 Explore planner 的 capture/run 接缝。关闭时不读取可选输入、
账本、凭证或传输模块；现有权限、执行、验收与结算权不变。

D1 分开判断目标关系与证据增量，缺历史则增量未知；D3 缺引用不认定支持；D4 保留
全部候选及必读来源；D5 不安装技能；D6 不提交计划。测试覆盖严格 JSON、重复字段、
部分未知、非法候选、缺证据、head 不匹配、来源变化、缓存撤销、超时、预算、实际
CLI、File/SQLite 和九项独立基线关闭差分。计时细分和请求期限也有回归保护。

续作本地使用 Python 3.11.7、Node 22.23.2、pytest 9.1.1；原验证使用过 Python
3.13.5，CI 使用 3.13。更晚的提交是否通过应看对应 head 的 CI。初次使用 Node
25.2.1 时 SQLite 资格失败；只改 PATH 仍复用了旧服务。独立 TMPDIR 和合格 Node
解决了运行环境问题，没有重启其他 Goal 的服务或放宽资格要求。

## 有限判断对照

21 个预先标注的合成案例，每个模型每例独立运行两次，共 42 次 Jev 和 42 次 Codex
无工具有限判断。材料和问题含义一致，API、系统提示和宿主开销不同。重复调用不是
独立任务；合成标签一致率不是生产准确率。

| 方向 | 每模型调用数 | Jev 输出完全匹配预设标签 | Codex 匹配 |
| --- | ---: | ---: | ---: |
| D1 进展偏离 | 12 | 9/12 | 12/12 |
| D2 职责复用 | 6 | 4/6 | 6/6 |
| D3 声明核对 | 6 | 6/6 | 4/6 |
| D4 材料排序 | 6 | 6/6 | 6/6 |
| D5 技能建议 | 6 | 6/6 | 6/6 |
| D6 重规划 | 6 | 6/6 | 6/6 |

Jev 有效输出 37/42 完全一致，原始最高概率标签为 38/42；没有降低原有 0.6 阈值来
改善数字。两次漂移案例弃权，一次目标变更案例的增量未知。D2 两次“相关但不同”
与预设“不相关”不一致，但未建议错误复用。Codex 两次把过度声明分为“证据不足”
而非“矛盾”，也没有认可声明。未知可能是正确答案，标签分歧不等于实际后果。
确定判断覆盖 Jev 61/76 项、Codex 64/76 项。

每次 Jev 成功响应核对固定 `jev-1.13.0`；Codex 请求 `gpt-6-astra`、medium，CLI
不暴露固定的实际模型小版本。币种费用未知。

## 真实工作对照

六个合成项目分别从相同初始文件运行 off/assist，共 12 次真实 Codex 工作会话。
所有产物通过独立契约验收；在副本中破坏产物时，12 个验收程序都拒绝。还核对了
helper/实验实际执行和只读证据未被修改。两边均为 6/6 完成，**没有观察到额外的成功
案例**。开启组有快有慢，每组仅一次正式测量，不能证明效率提升。

assist 使用该初始目录的真实评估，Agent 可以继续调查或拒绝建议。这是显式试验交接，
不是已安装的原生 Goal hook。前期探索批存在日志可见和输出类型未明确的问题，已排除
在正式效果/耗时统计外；正式批隔离日志与仓库、固定明确契约，并重新评估两个变更的
目标依据，没有复用旧建议。

D7/D8 另外四次真实调用均符合预设：原目标下正确改选，目标改变使原 baseline 更适合
时正确保持原选择。这不代表全系统调度收益。本轮扩展共 54 次 Jev 请求：42 次有限
对照、4 次排序、6 次初始工作判断、2 次契约明确后的重评。无自动重试，缓存不算新
试验。更早的 D7/D8 调用独立作为历史证据；Claude 排除在本轮范围外。

## 耗时与复现

完整有限判断客户端耗时中位数/P95：Jev 1952.985/3531.167 ms，Codex
10324.081/17424.028 ms，P95 使用 nearest-rank。Jev 请求到响应头中位数
1205.551 ms，包含 DNS/TLS、网络、排队和执行，不能称为纯推理耗时。系统输入和宿主
开销明显不同，不能据此证明纯模型速度或币种成本优势。

[示例与计时说明](examples/advisory/README.zh-CN.md)列出开关、预算、输入和阶段含义。
纳秒采样不等于物理纳秒精度；父子阶段不能重复相加；缓存的历史用量单独标明。
宿主执行、独立验收另计，人工组织材料耗时未知。公共示例和持久回归纳入仓库，原始
试验驱动器、轨迹和逐次记录不进入产品或 CI。英文版给出复现命令；缺少独立基线时
差分测试跳过，不算通过。

不声明统计上的质量/成本提升、PostgreSQL 资格、全仓认证、长期故障恢复或上游采用。
